import Modal from '@material-ui/core/Modal';

export const ModalShell = ({ open, handleClose }) => {
    return (
    <Modal
        open={open}
        onClose={handleClose}
    >
        lols qharhar
    </Modal>)
}